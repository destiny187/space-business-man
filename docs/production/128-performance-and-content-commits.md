# 최적화·콘텐츠 보강 순차 작업

2026-09-11 사용자 승인: 조사 보고서의 개선을 순서대로 구현하고, 확인한 변경마다 개별 커밋한다. 현재 원정의 INK v1·직접 운송·종별 원산지·기존 저장 보존을 유지한다.

## 순서와 상태

| 단계 | 범위 | 상태 |
| --- | --- | --- |
| O01 | 먼 거리 내부 윤곽 계산 생략 | 실제 렌더 비교 완료 |
| O02 | 지형 설치·해제의 순간 부하 분산 | 실제 지상 실행 완료 |
| O03 | 거래 복사·저장 비용 축소 | 거래·저장 격리 확인 완료 |
| O04 | 협동 상태 공통 자료·변경분 전송 | 손실 복구·로컬 연결 확인 완료 |
| O05 | 생물 생성·원거리 모션 비용 | 생성 분산·LOD 렌더 확인 · 리깅 후속 |
| O06 | 일반 생물 배회·먹이·회피 행동 | 현장 행동·채집·저장 확인 완료 |
| O07 | 표본의 기능 특성과 복원 활용 | 연구·이식·복원 처리·저장 확인 완료 |
| O08 | T3 사건·T4 생산/복원 한 묶음 | T3 사건·첫 T4 처리/계약 완료 |
| O09 | 원거리 사건·심층 지하 경관/보상 | 가장자리 완료 · 심층 진행 |
| O10 | 교역 수요·재방문 동기 | 대기 |

전체 완료를 선행 기록하지 않는다. 현재 작업 폴더에 있던 8,000종 제작·행성 표면·T3 준비 등의 다른 변경은 보존하며, 이 작업이 추가한 변경만 커밋한다. 단계의 예시 수치·세부 구현은 조정 가능한 초기값이다.

## O01 — 먼 거리의 내부 선

깊이 55m 이후 내부 명암선의 기여도가 0일 때 여덟 방향 법선·깊이와 중앙 법선의 읽기를 생략한다. 가까운 내부 선, 외곽선, 깊이/폭/색 기준과 투명 합성 순서는 유지한다. 설정의 실제 `crease_fade.y`를 읽으므로 다른 거리 기준도 따른다.

Godot 4.7.2 Metal Forward+ / Apple M2 / 960×640에서 실제 채광 로봇 모델 네 개를 원근/정사영과 거리 12/54/56/85m로 렌더했다. 변경 전 셰이더와 8개 조건의 2픽셀 간격 RGB 표본 차이는 모두 0이었다. 대표 근거리와 원거리 이미지를 직접 확인했다. 이는 이미지 동등성 확인이며 전체 게임 FPS 개선율 측정이 아니다.

검수 자료: `output/optimization-20260911/contours.json`, `contours.log`, `contour-*.png`. 게임 자산 원본·형상·음원은 변경하지 않았다. 이후 단계는 필요한 실행·화면·소리 확인만 수행하고 별도 버그가 있을 때 관련 검사를 좁혀 실행한다.

추가로 기존 경사면·투명 배치 고스트·실제 풀링 효과의 GPU 확인 3개를 실행해 실패 0을 확인했다(composition.log).


## O02 — 지형 청크 해제 분산

관심 구역 변경 시 바깥 청크를 즉시 전부 해제하지 않고 대기열에 둔다. 프레임당 최대 두 건의 분리/해제를 기존 3ms 설치 예산 안에서 시작하며, 다시 관심 구역에 들어온 청크는 보존한다. 굴착으로 교체된 청크는 시각·충돌·가림막을 함께 분리한 뒤 자원 해제를 다음 프레임들로 넘긴다. 충돌 준비 후 예산을 재확인하고, 굴착 묶음의 원자적 교체는 유지한다.

새 검사용 T1 원정에서 실제 Forward+ 지상 진입, 35.2m 이동, 관심 구역 변경과 해제 대기열 소진, 굴착 묶음 교체, 현재 채집기와 음원/효과 연결, 저장까지 9개 확인에서 실패 0이었다. 이동 전후와 굴착 화면을 직접 확인했다. 이 검사는 음원의 청취 검사가 아니며 일반 행동 규칙은 변경하지 않았다.

검수 자료: `output/optimization-20260911/field-retire.log`, `field/terrain.json`, `field/walk.png`, `field/excavation.png`. 이전 검사용 저장은 최신 생태 원형과 맞지 않아 사용하지 않고 새 저장을 만들었다. 사용자 저장은 건드리지 않았다.

한계: 엔진의 단일 자원 생성/해제 호출과 굴착 묶음 교체를 중간에 끊지는 않는다. 이번 실행의 지형 메인 처리 최대는 39.499ms, 개별 교체 최대는 2.629ms였다. 모든 프레임이 3ms 이하라는 의미가 아니며 지형 순간 지연의 완전한 해결로 기록하지 않는다.


## O03 — 거래 초안의 은하 원형 공유

호스트가 보유하는 은하 manifest를 별도 소유본으로 만들고 내부 배열·사전까지 읽기 전용으로 고정한다. 거래와 체크포인트는 재고·행동·진행·영수증을 독립 복사하고 이 불변 원형만 공유한다. 동일 참조의 해시·JSON을 최대 네 개까지 재사용한다. 외부의 가변 원형, 다른 참조, 캐시에서 퇴출된 원형은 기존의 전체 복사/해시 경로로 돌아간다. 저장 버전·내용과 검증 항목은 유지한다.

거래는 여전히 임시 파일 쓰기·읽기 비교·JSON 검사·백업·교체를 끝낸 뒤 성공한다. 체크포인트 작업자는 메인 스레드에서 포착한 원형 문자열과 독립 진행 상태만 받으며, 공유 캐시에 접근하지 않는다. JSON 필드 순서는 달라질 수 있지만 복원된 상태는 동일하다.

검사: 기존 백그라운드 저장의 순서·백업·손상 복구·I/O 실패 검사와 새 불변 원형/초안 격리 검사 각각 14개에서 실패 0. 후자는 실제 호스트 준비 거래의 저장 완료, 저장 실패 시 상태/영수증 불변, 체크포인트 격리, 잘못된 원형 교체의 거부와 캐시 퇴출을 포함한다. `tests/check_world_snapshot.gd`에 저장 무결성과 관련된 작은 검사를 보존한다.

검수 자료: `output/optimization-20260911/background-save.log`, `world-snapshot.log`. 같은 새 원정(원형 JSON 26,958자)의 복사 30회는 기존 7,333µs, 공유 방식 259µs였다. 성장한 세계의 전체 저장 지연이나 FPS 개선율은 측정하지 않았다. 변경되는 사업·생태 상태는 계속 독립 복사하며 디스크 쓰기도 유지한다.


## O04 — 공통 계산과 확인된 기준 상태의 변경분

한 번의 상태 발행에서 선박·연구·모션·공급 현황 등의 공통 자료를 한 번 계산하고 참가자별 시점·재고·FINCH 맥락을 따로 구성한다. 송신자와 수신자는 최근 여덟 상태만 보존하며, 수신자가 검증 후 확인한 순번에 대해서만 중첩 사전 변경/삭제를 전송한다. 확인되지 않은 순번은 기준으로 사용하지 않는다. 기준이 만료되면 다음 발행부터 전체 상태를 보내고, 이전 세션의 기준은 재사용하지 않는다.

900바이트 조각과 완성된 프레임의 검증, 최대 전송 크기는 유지한다. 변경분이 전체보다 크면 전체를 보낸다. 배열은 부분 인덱스 갱신 대신 값 전체를 교체한다. 네트워크 프로토콜만 4로 올려 이전 실행판과의 혼합 접속을 차단하며 세계 저장 버전은 유지한다.

최소 확인: 실제 현재 호스트 상태로 조각 역순 수신, 중간 프레임 누락, 필드 삭제, 변형된 패치 거부, 수신 화면과 기준 상태의 격리, 만료 후 전체 복구 등 15개에서 실패 0. 같은 이동 상태 10회의 압축 페이로드는 기존 25,715바이트, 변경분 1,961바이트였다. 별도 로컬 ENet 호스트/참가자의 대기실 합류·시작·변경분 수신·종료도 통과했고 최대 조각은 900바이트였다.

검수 자료: `output/optimization-20260911/snapshot-delta.log`, `network/host-result.json`, `network/guest-result.json`. 첫 연결 확인 도구가 대기실 상태를 너무 일찍 종료 판정해 이를 바로잡고 다시 실행했다. 인터넷 지연·6인 장시간 플레이는 실행하지 않았다. 크기 제한을 위해 전체 직렬화도 한 번 수행하며, 최근 기준의 독립 사본을 유지하므로 CPU·메모리 비용이 없어지는 최적화는 아니다.


## O05 — 생물 모델 생성 프레임 분리

현재 지상 스트리밍은 비동기로 받은 PackedScene을 직접 전달하고, 근거리 모델을 먼저 생성한 뒤 원거리 모델은 다음 생성 예산에서 완성한다. 기존 프레임당 생물 생성 수를 근거리/원거리 모델이 공유한다. 대기 중에도 근거리 모델을 표시하며, 원거리 완성 시 현재 거리 선택·외형·동작을 적용한다. 같은 LOD의 표시 상태를 매 프레임 다시 쓰지 않고 공격 타이밍 JSON도 재사용한다. 도감·사건 등 기존 호출의 즉시 두 모델 생성 방식은 유지한다.

실제 Forward+에서 동물/식물 두 종류의 근거리/원거리와 이동/섭식 동작 8개를 변경 전 코드와 비교했다. 같은 모션 시각에 고정한 렌더의 3픽셀 간격 RGB 표본 오차는 모두 0이었다. 지연 생성 중 표시 유지와 완성 후 LOD 전환 6개도 통과했다. 생명이 있는 별도 T1 원정에 들어가 현장 생물 네 개의 두 모델이 완성되는 것을 확인하고 화면을 직접 검수했다. 이 실행의 근거리 생성 최대는 4.851ms, 원거리 완성 최대는 0.921ms였다.

검수 자료: `output/optimization-20260911/deferred-lods.log`의 모델 비교, `field-lods.log`의 현장 확인, `field/lod-*.png`, `living/field-lods.png`. 초기 렌더 비교 도구는 숨겨진 모델의 시간이 진행되고 있어 동일 시각 고정을 바로잡았다. 최초 지형 검사용 행성은 정상적인 무생물 행성이므로, 모델 완성 확인은 생명권이 있는 새 검사용 행성으로 옮겼다. 무생물 행성의 규칙이나 저장을 변경하지 않았다.

단일 GLB의 노드 생성 자체를 중간에 나누지는 않는다. 이 커밋은 모델·모션 품질이나 모션 갱신률을 낮추지 않는다. 별도 작업 중인 8,000종 리깅 코드의 반복 뼈 검색과 더 가벼운 전용 원거리 자산은 이번 커밋에 포함하지 않았다.


## O06 — 일반 지상 동물의 행동

활성 지상 동물은 지형과 몸의 여유 공간을 검사한 짧은 경로에서 배회·섭식·휴식을 반복한다. 같은 지역의 같은 종은 방향과 행동 주기를 공유한다. 가까운 승무원이 있으면 검증된 경로 안에서 거리를 벌리거나 경계하며 기존 동작·울음·지면 효과로 반응한다. 이 단계의 군집은 공유 방향/주기이며 먹이사슬·번식·목적지 탐색·전면적인 무리 AI는 아니다.

호스트의 현재 시간·동일 행성의 접속 승무원 위치로 행동을 계산한다. 클라이언트는 수신 시각 사이를 최대 0.1초까지 보간하며, 실제 이동 충돌체와 스캔·공격·표본 채집 판정이 같은 경로를 사용한다. FINCH의 독립 출동도 해당 행성의 승무원만 반응에 포함한다. 휴면·수송 후 격리 표본은 기존 위치를 유지하며, 현재 별도 작업 중인 조류 비행과 대기/수중 생물을 지상 배회로 바꾸지 않는다. 종 ID·원산지·등장 확률은 유지한다.

무력화된 동물은 명중한 위치에 멈춘다. 선택 필드 `crew.wildlife_stops`는 기존 체력 기록과 함께 위치·방향을 저장하고 검증한다. 정지 기록이 없는 이전 무력화 개체는 기존 생성 위치를 유지한다. 표본 채집 성공 시 정지 위치를 정리하고, FINCH 거래도 같은 상태를 확정한다. 세계 저장 버전을 올리거나 기존 지형·혈통을 다시 만들지 않는다.

최소 확인: 실제 지형에서 행동 주기·이동·독립 필드의 동일 위치·휴면/이식 경계·이동 표적의 호스트 판정 등 10개, 실제 Forward+ 게임 창에서 이동·충돌·E 스캔·근접 회피·펄스 무기 무력화·정지·저장/재로드 등 13개에서 실패 0. 재로드한 무력화 개체의 호스트 조준·직접 표본 채집·정지 기록 정리·저장도 통과했다. 대표 이동/회피/정지 화면을 직접 확인했다.

기존 Blender 생물과 `sfx_creature_call` 음원을 재사용했다. 실제 재생과 SFX 믹서 신호를 확인하고 약 1.5초를 WAV로 포착했다(최대 절댓값 0.743). 이 도구에서는 오디오 입력 청취가 지원되지 않아 음색의 직접 청취 검수로 기록하지 않는다. 메뉴/입력 차단 중에는 새 반응 음원·효과 호출을 차단하는 기존 앱 상태를 따른다.

검수 자료: `output/optimization-20260911/wildlife-rules.log`, `wildlife-play.log`, `stopped-collect.log`, `wildlife/wildlife-*.png`, `wildlife/wildlife-alert.wav`. 지형 검사를 통과하지 못하는 위치는 정지/섭식만 수행한다. 단거리 회피 범위 밖의 장애물 우회, 정교한 발 IK, 종별 새 음원, 6인 인터넷 플레이는 이번 확인 범위에 포함하지 않는다.

## O07 — 종별 기능 분석과 현장 복원

서식 환경의 기초 분석 후 실물 표본을 아이템창에 보유하고 공동 원정선 연구대에서 종별 기능을 분석한다. 실험용 광물 4개를 사용하며 표본은 보존한다. 기존 환경별 공학 연구가 선택한 첫 표본과 개조 자격을 바꾸지 않는다. FINCH에는 연구대 기능을 추가하지 않는다.

종 ID로 정한 여섯 기능(기질 순환·축열 공생·수분 보유·천이 촉진·미세 여과·배양 보조)은 세 대응 설비(생태 배양기·온도 조절기·수자원 처리기)의 처리 속도를 초기값 6/8/10% 보조한다. 연구한 종을 다른 행성에 이식하고, 표본과 설비가 지원 중인 호환 구획에 있을 때 적용한다. 설비는 구획 내부·표본 24m 이내여야 하며 가장 강한 한 종만 반영한다. 지원 종료·다른 행성·범위 이탈·서식 부적합에는 효과를 주지 않는다. 색·크기 변이는 새 기능 또는 기본종으로 계산하지 않는다.

현재 자유 면적 복원과 기존 시설 처리 경로에 효과를 연결하고, 수자원 처리 주기의 변화는 T3 직접 운반용 보급 계산에도 반영한다. 배양기의 동일 시간 투입 비용은 유지하고, 수자원 처리기는 빨라진 주기만큼 얼음도 사용한다. 공중/수중 생물의 기능 연구·새 전문 모델·복잡한 먹이망·전 종 밸런스는 이번 범위가 아니다.

선택 저장 필드 ecology.species_research는 종→실물 표본 ID만 보존한다. 미관측/다른 종/실물 근거 없는 연구를 거부하고 현재 표본의 운반·이식 원장을 그대로 검증한다. 주변·운반·이식 종의 완료 여부만 현장 복제에 넣고, 먼 행성의 도감 조회에도 연구 여부를 제공한다. 연구대는 표본 아이콘→기존 Blender 설비의 렌더 카드, 실제 생물 미리보기, 분석 전후 색·버튼 상태로 관계를 보여 준다.

최소 확인: 실제 Forward+ 연구대에서 표본 회수·보유 조건·호스트 분석·단일 비용 차감·기존 공학 증거 보존·표본 보존·현장 복제·저장/재로드 11개에서 실패 0. 별도 저장의 실제 표본을 다른 행성에 이식한 뒤 출처/원장/저장·거리/지원 종료·중복 비중첩·기존 및 자유 면적 처리량 비교 등 13개에서 실패 0이었다. 확인한 갑각류는 같은 투입으로 배양량 1.06배를 보였다. 전체 게임 FPS나 전 종 밸런스 측정은 아니다.

이식 개체와 기존 배양기 모델이 있는 별도 화면 검사용 저장을 Forward+로 열고, 연구대의 960×640 표시를 포함한 5개 화면/저장 확인에서 실패 0이었다. 설비는 렌더 확인용으로 준비했으며 새 게임에 자동 배치한 것이 아니다. 기존 연구 성공 음원/효과의 호스트 응답 경로를 재사용하며 새 음원 제작·직접 청취 검수는 하지 않았다.

검수 자료: output/optimization-20260911/species-play.log, species-support.log, species-view.log 및 species/·species-view/의 화면. 첫 실행 도구는 재접속 시 회수 화물로 옮겨지는 기존 표본 규칙을 빠뜨려 실제 회수 작업을 추가했다. 처리량 확인 도구의 불완전한 셀 자료를 보완해 관련 검사를 다시 실행했다. 사용자 저장은 수정하지 않았다.


## O08A — T3 폭풍 기록고

방전 전조·범위/지붕 엄폐 → 구리 회로 수리 → 실물 배터리 운반 → 접지 → 도구 해치 개방 → 걸어서 내부 기록 회수 → 공동 T3 설계도 해금을 연결했다. 이미 가진 설계도는 정해진 가공 부품으로 회수한다. 기존 사건의 타일 난수열을 유지하는 독립 생성 경로를 사용한다. 기존 Blender 난파선·장치·가동부와 ElevenLabs 사건 경보/펄스 음원 및 효과를 재사용했다. 음색 직접 청취 검수는 하지 않았다.

실제 플레이에서 기존 난파선 갑판의 26cm 턱이 보행 진입을 막는 문제를 확인해 Blender 원본에 강철 경사판을 추가하고 GLB를 다시 내보냈다. Blender Cycles와 Godot Forward+에서 결과를 직접 확인했으며 해치 충돌·개방 동작을 유지했다. 해치까지의 실물 운반/수리는 `storm-play-through-hatch.log`, 수정 후 진입·회수·사용권·저장/재로드·중복 거부 11개는 `storm-entry.log`에 기록했다. 최초 턱 진입 실패를 전체 플레이 통과로 합산하지 않는다.

기존 사건 네 타일의 동일성·기본 위험 판정 등 초기 9개 확인을 통과했다. 실제 복제에서 짧은 방전 단계가 생략되는 문제가 있어 확정 방전 횟수를 전달하고 최소 표시 시간을 유지했다. 최종 GPU 실행에서 정확한 지붕 조각 판정·도감 해금 상태·생략된 단계의 방전 표시 등 6개를 통과했다. 도감에는 기존 난파선 이미지 카드를 연결한다.

검수 자료: `output/optimization-20260911/storm/`, `storm-fx/`의 Blender/게임 이미지, `storm-prepare.log`, `storm-entry.log`, `storm-fx.log`. 초기 화면 도구의 로딩 종료·캡처 시점 문제를 바로잡았으며, 최종 FX 실행은 종료 누수 경고 없이 끝났다. 앞선 진입 실행의 ObjectDB 종료 경고는 별도로 남긴다. T3 도달 시간·6인 인터넷·모든 시드 검사는 하지 않았다. 전역 날씨·침수 기록실·T4/T5 전용 사건은 이번 범위가 아니다.


## O08B — T4 극한 유입과 전문 처리 팩

새 은하의 regional_rules v4에 T4 고열/극저온 유입, 73,728m²·60초 자유 면적 계약을 연결했다. 구역 내부의 Mk.3 열교환기는 실제 전용 팩 운전 시간으로 온도·잔류 오염을 함께 처리한다. Mk.1/Mk.2 또는 빈 보급으로 극한 처리를 우회할 수 없다. 별도의 원인 제어 장치가 팩을 사용해 유입량과 온도 재악화를 줄인다. 구역 밖의 기초 설비는 기존 복원 역할을 유지한다.

극열 흡수 팩과 극저온 완충 팩은 각각 기존 T4 원광과 전문 부품으로 Mk.3 제작소에서 생산한다. 다른 행성에서 원료/팩을 직접 운송할 수 있다. 기존 Blender 열전달 제품·Mk.3 설비·가동부와 현재 시설 피드백 경로를 재사용했으며 새 모델·음원 제작 또는 Mk.4 설비로 기록하지 않는다. 기존 은하는 저장된 지역 규칙을 유지한다.

최소 확인: 새 T4 규칙·구 세계 경계·지원 지면·팩 소비/잔여 시간·열 유입/억제·초기 저장 18개, 실제 Forward+ 창의 제작소 생산에서 이어진 재고 차감·출고·현장 가동·오염 감소·저장/재로드 12개, 근접 설비/메뉴/유입원 렌더 6개에서 실패 0이었다. 기존 제품 이미지·3D 미리보기·가동 설비를 직접 확인했다. 초기 생산 요청 성공은 tier4-production-start.log에 남겼다. 도구가 거래 이전 사본을 읽던 오류와 재접속 시작 위치/출항 상태를 바로잡고, 이미 지급된 생산부터 이어서 확인했다.

마감 상태를 준비한 별도 계약 자료에서 59초 거부/60초 완료·보존식·단계 지급·중복 지급 방지·최종 총액·저장 등 9개를 확인했다. 기후와 정착을 준비한 마감 검사이며 새 게임부터 T4까지의 전체 소요 시간이나 모든 복원 과정을 실행한 것은 아니다. 실제 렌더 대표는 고열 유형이며 극저온의 별도 장시간/전 시드/협동 검사는 하지 않았다. 앞선 실행의 ObjectDB 종료 경고 1개는 남겼고 최종 근접 렌더는 해당 경고 없이 종료했다.

검수 자료: output/optimization-20260911/tier4-setup.log, tier4-production-start.log, tier4-play.log, tier4-close.log, tier4-view.log, tier4/의 이미지와 별도 저장. T4의 모든 극한 환경/내구도/전용 구조물·T5 광역망·자동 운송은 이번 완료 범위가 아니다.


## O09A — 탐사 가장자리 사건과 생물 식별 경계

현재 지표 이동 범위는 각 축 ±8,192m이며 사건 생성의 고정 8,000m 경계가 먼저 끝나 있었다. 기존 난수열·장소를 유지하고 별도 시드/ID의 난파선·운반/동력 사건을 바깥 띠에 추가했다. 실제 이동 경계에서 80m를 비워 배터리·중계기·필요 동선이 밖으로 나가지 않게 한다. 새 탐사 가능 면적을 임의로 늘리지는 않는다.

생물 식별의 274칸 상수는 저장된 자원 영역과 현재 실제 배치 셀 간격으로 계산한다. 현재 세계의 유효 범위는 동일하며 이미 관측한 경계 표본의 여유 범위를 유지한다. 신규 사건은 기존 원거리 타일 캐시에 함께 보관하고 동시에 표시할 모델/관심 거리 제한을 유지한다.


현재 범위의 생물 식별·기존 가장자리 사건 비교·신규 사건/상호작용 경계·저장 등 12개, 실제 Forward+ 창에서 지형/모델 준비·가장자리 보행·저장/재로드 8개를 확인했다. 대표는 x=8,064m의 동력 복구 난파선이며 기존 Blender 장치와 사건 피드백을 재사용한다. 초기 실행 도구의 타일 크기 가정(실제 640m)을 바로잡았고, 이동 검사는 모든 충돌 자산 준비 후 평탄한 접근 경로에서 다시 확인했다. 전역 이동 범위 확대·다른 행성 새 날씨·전체 사건 반복 검사는 하지 않았다.

검수 자료: output/optimization-20260911/edge-setup.log, edge-play.log, edge/edge-walk.png와 별도 저장.
