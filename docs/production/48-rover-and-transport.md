# 로버 제작·지상 운용·행성 간 운송

설계 기준은 [로버·운송](../game/16-rover-and-transport.md), 작업 구분은 [C13~C15](../planning/11-ground-commit-plan.md)이다. 전체 UI 재설계는 별도이며 실제 모델과 짧은 문맥 조작을 우선한다.

## C13 — 제작 자산

2026-09-08. Blender 5.2.1에서 SCOUT 2인승 밀폐형 바퀴 로버를 제작했다. 원본은 `art/blender/vehicles/scout_rover.blend`, 게임 모델은 `assets/models/vehicles/scout_rover.glb`다. `tools/build_scout_rover.py`로 재생성한다. 두 좌석·유리문, 전후 바퀴, 조향·서스펜션, 화물 덮개와 고정점 등 25개 동작/연결 노드를 제공한다.

Blender 렌더와 Godot 4.7.2 Forward+ / Metal의 INK v1 정면·후면·문/화물 개방·운전석 시야를 확인했다. 검수 이미지는 `media/rover/`, 실행 스크립트는 `tests/render_scout_rover.gd`다. 대시보드는 전방 시야 아래에 배치했다. 제작 자산 커밋에서 실제 게임에 미완성 제작 버튼을 노출하지 않는다.

ElevenLabs Sound Effects에서 주행 반복·시동·문·제동·적재 윈치 반복·고장 6종을 생성했다. 원음은 `audio/source/elevenlabs/`, 실제 WAV는 `assets/audio/`, 프롬프트·파일 해시·길이·가공 정보는 `audio/manifests/rover-sounds.json`에 보존했다. `tools/import_rover_audio.py`는 모노화·DC 제거·주파수 정리·루프 접합·음량 조정을 수행한다. 파일 신호와 길이를 확인했으며 청감 평가는 아직 하지 않았다. 실제 게임의 공간 재생 연결은 C14/C15에서 기록한다.

## C14 — 지상 로버

개인 현장 물류 I(기초 로봇공학·가방 재료), 가동 제작소의 공동 부품 예약/45초 조립/안전 출고, 호스트 주행·두 좌석, 안전 하차, 화물 4칸의 클릭/드래그 전송과 장비 소유권, 충전·수리·구조 충전·전복 복구를 연결했다. 데이터는 `data/rovers.json`, 소유 원장은 `world.rovers`다. 구 세이브의 차량 기본값은 없음이며 좌석·작업 예약은 영구 저장하지 않는다.

F는 문/후방 화물 문맥 조작, WASD는 주행, Space는 제동, V는 운전석/추적 시점이다. 동승자는 E로 스캔하고 정차 후 X로 빈 운전석에 이동한다. 차량 키는 기존 입력 재지정에 연결했다. 메뉴·입력 만료·운전자 이탈은 제동하며 차량 안에서는 개인 채광·공격·건설을 호스트가 거절한다. 실제 모델의 바퀴·조향·서스펜션·문/화물 움직임과 ElevenLabs 공간 음원, 좌석 자세, 운전 계기를 연결했다.

Godot Forward+ 실제 창에서 `tests/check_rover_ground.gd`로 연구→시설→실제 45초 제작→운전석/추적 주행→메뉴 제동/음원 정지→하차→클릭 화물→수리/구조 충전→저장/재읽기를 확인했다. 주행 루프의 재생 상태를 확인했으나 청감 평가는 미확인이다. 960×640 화물창과 1280×800 연구·제작·운전 화면을 검수했다. 별도 좌석/복구 검사에서 호스트 권한에 두 참가자를 넣어 같은 좌석 선점 거절·동승 스캔·개인 공격 차단·운전자 이탈 제동/점유 해제, 실제 3초 유지 복구와 전력 있는 충전 패드를 확인했다. 실제 두 클라이언트의 네트워크 주행이나 장거리/다양한 경사 주행은 실시하지 않았다.

검사 중 차량 화물의 필수 재고 키, 직접 진입 시 입력 바인딩 누락, 연구 모델 미리보기의 세로 늘어남, 복구 시작 시 입력 도착 여유와 안전 위치 후보 탐색을 수정했다. 테스트의 가짜 참가자 RPC 경고와 임의 운전자 이탈 후 화면 전환 상태는 검사 도구에서 분리했다. 차량 출고/복구의 실제 충돌 공간은 현재 로드된 지형에서 검사한다.

## C15 — 강화와 한 대 운송

Mk.2(현장 가방 부품·20초 정비), 개인 현장 물류 II(적재/하역 8→5초), 기본 선체에도 제작 가능한 차량 적재 개조 I(공동 창고 부품·한 대·6 운송 단위)을 연결했다. Mk.2는 속도 13.8m/s·배터리 상한 150·내구 상한 400이며 기존 소모·손상은 유지한다. 부품은 정비 완료 시 재확인하여 한 번 소비하므로 취소·연결 종료로 예약 재료를 잃지 않는다. 4칸은 유지한다.

차량 뒤쪽 화물창에서 정비/적재를, 착륙선 단말 또는 선박 정비의 모델 카드에서 적재함을 연다. 적재함은 화면 오른쪽에 두고, 하역 위치의 실물 크기 고스트를 나머지 화면에 보여 준다. 실제 예약 진행률·윈치/차량 이동·ElevenLabs 윈치 루프를 연결했다. 완료 전에 성공으로 처리하지 않는다. 준비·작업·완료/거절의 화면과 소리를 구분하고 메뉴 중 재생을 차단한다.

`world.rovers.transport_level/ship_vehicle`는 로봇 격납고·기존 선체 mass와 별도다. 차량 본체와 화물·장비를 단일 ID에 묶어 위치만 변경하며 일반 선박 창고에 복사하지 않는다. 선체·일반 모듈 교체는 이 적재 개조와 실물을 지우지 않는다. 적재/하역 예약은 한 건만 허용하고 탑승·화물 조작을 잠근다. 이동·피해·담당자 이탈·출항 준비로 취소되면 원래 위치에 남는다. 하역 완료 때 지지 지형과 실제 충돌 공간을 재확인한다. 지상에 남긴 차량은 해당 행성에 보존하고 출항 단말에 차량 아이콘과 남길 대수를 표시한다. 로버 승차는 우주선 탑승으로 세지 않는다.

실제 창에서 20초 개조, 자원 300개+개인 장비 1개의 4.8단위 만재, 8초 적재, 피해 취소·중복 화물 조작 거절, 윈치 루프 재생, 선내 저장 재읽기, 물류 II, 정상 전원 승선/이륙을 확인했다. 다음 행성 착륙 후 5초 하역 중 실제 충돌체를 추가해 출고 차단/선내 보존을 확인하고, 공간 확보 후 동일 ID·Mk.2·화물·장비 소유권·내구·배터리로 하역했다. 남긴 다른 로버와 타인 장비 인수 거절, 하역 후 저장 재읽기도 확인했다. 실행 스크립트는 `check_rover_transport.gd`, 이미 실은 저장에서 이어가는 `check_rover_arrival.gd`다. 검사 스크립트의 이전 선택 행성 참조를 수정했고, 완료한 정비/적재를 반복하지 않고 저장에서 이어서 확인했다.

960×640 적재함과 실제 하역 고스트를 추가 검수했다. 원격 두 클라이언트 주행/적재 지연, 장거리·험지 승차감, 음색 청감, Windows 실기와 전체 성능 안정화는 미확인이다. 시드 낮밤(C16/C17)·다른 차량·운송 공간의 선체 질량 통합·정비 재료 할인은 이번 완료 범위 밖이다.

하역 고스트에 불필요하게 INK 재질을 만든 뒤 투명 재질로 즉시 교체하던 경로에서 종료 시 Metal 재질 참조 오류가 발생했다. 배치 고스트와 동일하게 원본 모델에 투명 재질을 직접 적용하도록 수정했다. 윈치 메시/재질도 매 프레임 새로 만들지 않고 재사용한다. 해당 적재/하역·960×640 화면·장면 정리만 재실행하여 렌더 오류가 사라진 것을 확인했다(`check_rover_transport_render.gd`).

운전석 카메라는 차체가 회전할 때 기존 마우스 관찰 각도를 유지한 채 함께 회전하도록 보완했다. `check_rover_camera.gd`에서 차체 방향 변경 전후의 관찰 각도와 안전 하차를 확인했다.

## 공통 아트 후속 보강

2026-09-08 [Blender 공통 제작 규격](49-common-blender-art.md)에 맞춰 SCOUT 재질·베벨 법선과 GLB 병합을 갱신했다. 기존 동작/좌석 연결점을 보존하고 실제 INK 기구·실내 렌더를 다시 확인한다. 기존 창고도 같은 기준으로 교체했다. 전 자산 형태 교체 완료를 의미하지 않는다.
